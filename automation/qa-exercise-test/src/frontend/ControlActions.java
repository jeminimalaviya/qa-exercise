package frontend;

import org.openqa.selenium.WebDriver;

public class ControlActions {
	
	// verify if Toggle all check box selects all individual check boxes of tasks
	public static boolean toggleAllVerified(WebDriver driver) {
		boolean verified = Boolean.valueOf(false);
		
		return verified;
	}
	
	// verify if the category selector and due date selectors visibility is toggled with [Advanced] click 
	public static boolean advanceControlVerified(WebDriver driver) {
		boolean verified = Boolean.valueOf(false);
		
		return verified;
	}
	
	// verify mouse hover over category shows "remove this category" text
	public static boolean categoryRemoveTextVerified(WebDriver driver) {
		boolean verified = Boolean.valueOf(false);
		
		return verified;
	}
	
	// verify that click on category leads to deletecat.php with respctive category
	public static boolean categoryClickVerified(WebDriver driver) {
		boolean verified = Boolean.valueOf(false);
		
		return verified;
	}

}
